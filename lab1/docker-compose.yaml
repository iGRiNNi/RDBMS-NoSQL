services:
  postgresql:
    image: postgres:17
    restart: always
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: oil_field
    ports:
      - 15432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: